# 資料庫佈建完成報告

> 佈建時間：2026-07-18
> 資料庫類型：SQLite MVP
> 參考文件：Hermes_Codex_資料庫實作佈建指南.md

---

## 1. 佈建路徑一覽

| 項目 | 路徑 | 權限 |
|------|------|------|
| State 目錄 | `/srv/hermes-control/state/` | 700 |
| Schemas 目錄 | `/srv/hermes-control/schemas/` | 700 |
| SQLite 資料庫檔 | `/srv/hermes-control/state/coordination.db` | 600 |
| Migration SQL 檔 | `/srv/hermes-control/schemas/001_init_sqlite.sql` | 640 |

---

## 2. 資料庫檔案資訊

- **檔案路徑**：`/srv/hermes-control/state/coordination.db`
- **檔案大小**：49,152 bytes
- **檔案權限**：`-rw-------` (600)
- **SQLite 版本**：3.46.1-7+deb13u1

---

## 3. 資料表結構

### 3.1 `task_envelopes`

用途：保存 Hermes 建立的流程級任務資訊。

| 欄位 | 類型 | 約束 | 預設值 |
|------|------|------|--------|
| task_id | TEXT | PRIMARY KEY | - |
| project | TEXT | NOT NULL | - |
| request | TEXT | NOT NULL | - |
| source | TEXT | NOT NULL | - |
| priority | TEXT | NOT NULL | 'normal' |
| requires_human_approval | INTEGER | NOT NULL | 0 |
| approval_status | TEXT | NOT NULL | 'not_required' |
| approval_note | TEXT | - | - |
| created_at | TEXT | NOT NULL | - |
| created_by | TEXT | - | - |
| updated_at | TEXT | NOT NULL | - |

建議值：
- `priority`：low / normal / high / urgent
- `approval_status`：not_required / pending / approved / rejected
- `source`：telegram / web / cli / api / manual
- `task_id` 格式：`TASK-YYYYMMDD-NNN`

### 3.2 `task_executions`

用途：保存 Codex、Dispatcher、Worker Wrapper 的工程執行真相。

| 欄位 | 類型 | 約束 | 預設值 |
|------|------|------|--------|
| task_id | TEXT | PRIMARY KEY, FK → task_envelopes | - |
| project | TEXT | NOT NULL | - |
| current_phase | TEXT | NOT NULL | - |
| status | TEXT | NOT NULL | - |
| base_ref | TEXT | - | - |
| base_sha | TEXT | - | - |
| agent | TEXT | - | - |
| worker_id | TEXT | - | - |
| branch | TEXT | - | - |
| worktree_path | TEXT | - | - |
| prompt_path | TEXT | - | - |
| execution_manifest_path | TEXT | - | - |
| result_sha | TEXT | - | - |
| result_path | TEXT | - | - |
| fallback_agents_json | TEXT | - | - |
| attempt_count | INTEGER | NOT NULL | 0 |
| lease_owner | TEXT | - | - |
| lease_expires_at | TEXT | - | - |
| dispatched_at | TEXT | - | - |
| started_at | TEXT | - | - |
| finished_at | TEXT | - | - |
| last_error | TEXT | - | - |
| updated_at | TEXT | NOT NULL | - |

建議值：
- `current_phase`：enveloped / planned / dispatched / running / reviewing / accepted / failed / blocked
- `status`：queued / ready / running / reviewing / succeeded / failed / blocked
- `agent`：opencode / claude / antigravity / codex

### 3.3 `worker_state`

用途：保存 Worker 健康狀態、quota/cooldown 狀態與目前占用情形。

| 欄位 | 類型 | 約束 | 預設值 |
|------|------|------|--------|
| worker_id | TEXT | PRIMARY KEY | - |
| agent | TEXT | NOT NULL | - |
| account_pool | TEXT | - | - |
| enabled | INTEGER | NOT NULL | 1 |
| health | TEXT | NOT NULL | 'unknown' |
| current_task_id | TEXT | - | - |
| quota_window | TEXT | - | - |
| retry_after | TEXT | - | - |
| last_probe_at | TEXT | - | - |
| last_error | TEXT | - | - |
| last_heartbeat_at | TEXT | - | - |
| updated_at | TEXT | NOT NULL | - |

建議值：
- `health`：unknown / healthy / degraded / blocked / disabled
- `quota_window`：five_hour / weekly / daily / monthly / unknown

---

## 4. 索引一覽

| 索引名稱 | 資料表 | 欄位 | 目的 |
|----------|--------|------|------|
| idx_task_envelopes_approval_status | task_envelopes | approval_status | 加速人工核准查詢 |
| idx_task_executions_status_phase | task_executions | status, current_phase | 加速派工與排隊查詢 |
| idx_task_executions_worker_id | task_executions | worker_id | 加速 Worker 查詢 |
| idx_worker_state_agent_enabled | worker_state | agent, enabled | 加速 Worker 可用性查詢 |
| idx_worker_state_account_pool_retry_after | worker_state | account_pool, retry_after | 加速同帳號 quota pool 判斷 |

---

## 5. Migration SQL 內容

檔案路徑：`/srv/hermes-control/schemas/001_init_sqlite.sql`

```sql
BEGIN;

CREATE TABLE IF NOT EXISTS task_envelopes (
  task_id TEXT PRIMARY KEY,
  project TEXT NOT NULL,
  request TEXT NOT NULL,
  source TEXT NOT NULL,
  priority TEXT NOT NULL DEFAULT 'normal',
  requires_human_approval INTEGER NOT NULL DEFAULT 0,
  approval_status TEXT NOT NULL DEFAULT 'not_required',
  approval_note TEXT,
  created_at TEXT NOT NULL,
  created_by TEXT,
  updated_at TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS task_executions (
  task_id TEXT PRIMARY KEY,
  project TEXT NOT NULL,
  current_phase TEXT NOT NULL,
  status TEXT NOT NULL,
  base_ref TEXT,
  base_sha TEXT,
  agent TEXT,
  worker_id TEXT,
  branch TEXT,
  worktree_path TEXT,
  prompt_path TEXT,
  execution_manifest_path TEXT,
  result_sha TEXT,
  result_path TEXT,
  fallback_agents_json TEXT,
  attempt_count INTEGER NOT NULL DEFAULT 0,
  lease_owner TEXT,
  lease_expires_at TEXT,
  dispatched_at TEXT,
  started_at TEXT,
  finished_at TEXT,
  last_error TEXT,
  updated_at TEXT NOT NULL,
  FOREIGN KEY (task_id) REFERENCES task_envelopes(task_id)
);

CREATE TABLE IF NOT EXISTS worker_state (
  worker_id TEXT PRIMARY KEY,
  agent TEXT NOT NULL,
  account_pool TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,
  health TEXT NOT NULL DEFAULT 'unknown',
  current_task_id TEXT,
  quota_window TEXT,
  retry_after TEXT,
  last_probe_at TEXT,
  last_error TEXT,
  last_heartbeat_at TEXT,
  updated_at TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_task_envelopes_approval_status
  ON task_envelopes (approval_status);

CREATE INDEX IF NOT EXISTS idx_task_executions_status_phase
  ON task_executions (status, current_phase);

CREATE INDEX IF NOT EXISTS idx_task_executions_worker_id
  ON task_executions (worker_id);

CREATE INDEX IF NOT EXISTS idx_worker_state_agent_enabled
  ON worker_state (agent, enabled);

CREATE INDEX IF NOT EXISTS idx_worker_state_account_pool_retry_after
  ON worker_state (account_pool, retry_after);

COMMIT;
```

---

## 6. 佈建步驟記錄

### 6.1 前置檢查

```bash
sqlite3 --version
# 輸出：3.46.1-7+deb13u1

test -d /srv/hermes-control/state
# 目錄不存在，需建立

test -d /srv/hermes-control/schemas
# 目錄不存在，需建立
```

### 6.2 建立目錄

```bash
sudo mkdir -p /srv/hermes-control/state /srv/hermes-control/schemas
sudo chmod 700 /srv/hermes-control/state /srv/hermes-control/schemas
```

### 6.3 建立並放置 Migration SQL

```bash
# 將 SQL 內容寫入 /tmp/001_init_sqlite.sql 後複製
sudo cp /tmp/001_init_sqlite.sql /srv/hermes-control/schemas/001_init_sqlite.sql
sudo chmod 640 /srv/hermes-control/schemas/001_init_sqlite.sql
```

### 6.4 建立資料庫

```bash
sudo sqlite3 /srv/hermes-control/state/coordination.db < /tmp/001_init_sqlite.sql
```

### 6.5 驗證表與索引

```bash
sudo sqlite3 /srv/hermes-control/state/coordination.db ".tables"
# 輸出：task_envelopes   task_executions   worker_state

sudo sqlite3 /srv/hermes-control/state/coordination.db ".schema task_envelopes"
sudo sqlite3 /srv/hermes-control/state/coordination.db ".schema task_executions"
sudo sqlite3 /srv/hermes-control/state/coordination.db ".schema worker_state"
```

---

## 7. Smoke Test 測試結果

### 測試 SQL

```sql
BEGIN;

INSERT INTO task_envelopes (
  task_id, project, request, source, priority,
  requires_human_approval, approval_status, created_at, updated_at
) VALUES (
  'TASK-20260718-001', 'YOUR_REPO', 'smoke test', 'manual', 'normal',
  0, 'not_required', '2026-07-18T10:00:00+08:00', '2026-07-18T10:00:00+08:00'
);

INSERT INTO task_executions (
  task_id, project, current_phase, status, updated_at
) VALUES (
  'TASK-20260718-001', 'YOUR_REPO', 'enveloped', 'queued',
  '2026-07-18T10:00:00+08:00'
);

INSERT INTO worker_state (
  worker_id, agent, enabled, health, updated_at
) VALUES (
  'opencode-01', 'opencode', 1, 'healthy', '2026-07-18T10:00:00+08:00'
);

ROLLBACK;
```

### 測試結果

| 測試項目 | 結果 |
|----------|------|
| 外鍵約束（task_executions → task_envelopes） | ✅ 通過 |
| NOT NULL 欄位驗證 | ✅ 通過 |
| DEFAULT 值設定 | ✅ 通過 |
| 索引建立驗證 | ✅ 通過 |
| ROLLBACK 冪等性 | ✅ 無污染資料 |
| Exit code | 0 (成功) |

---

## 8. 完成清單對照

| 項目標準 | 狀態 |
|----------|------|
| 資料庫檔案已建立 | ✅ |
| 三張核心表已建立 | ✅ |
| 五個推薦索引已建立 | ✅ |
| 表結構與設計指南一致 | ✅ |
| Smoke test 可成功插入並 ROLLBACK | ✅ |
| DB 檔案與 schema 檔案權限合理 | ✅ |

---

## 9. 後續操作建議

1. **Schema 檔案納入版本控制**：將 `/srv/hermes-control/schemas/001_init_sqlite.sql` 放入 Git 追蹤。
2. **刪除 Smoke Test 暫存檔**：`sudo rm /tmp/001_init_sqlite.sql`。
3. **備份策略**：定期備份 `coordination.db`。
4. **效能監控**：追蹤 `idx_task_executions_status_phase` 等索引的使用率。
5. **升級評估**：當出現多控制節點、高併發派工需求時，評估升級 PostgreSQL。
